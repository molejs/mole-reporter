var webpack = require('webpack');
var path = require('path');

module.exports = {
  entry: './src/mole-reporter.js',
  output: {
    path: path.join(__dirname, 'dist'),
    filename: 'mole-reporter.min.js',
    publicPath: '/dist/',
    libraryTarget: 'var',
    library: 'Mole'
  },
  externals: {
    'fetch': 'fetch'
  },
  resolve: {
    extensions: ['', '.js', '.jsx']
  },
  devtool: 'source-map',
  plugins: [
    new webpack.optimize.OccurenceOrderPlugin(),
    new webpack.optimize.UglifyJsPlugin({
      compress: {
        warnings: false
      }
    })
  ],
  module: {
    loaders: [
      {
        test: /\.jsx?$/,
        loaders: ['babel'],
        include: path.join(__dirname, 'src')
      }
    ]
  }
};